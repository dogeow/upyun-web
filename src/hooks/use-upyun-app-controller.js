import { useUpyunActions } from '@/hooks/upyun/use-upyun-actions'
import { useUpyunEffects } from '@/hooks/upyun/use-upyun-effects'
import { useUpyunState } from '@/hooks/upyun/use-upyun-state'

export function useUpyunAppController() {
  const state = useUpyunState()
  const actions = useUpyunActions(state)

  useUpyunEffects({
    state,
    loadDirectory: actions.loadDirectory,
    clearSession: actions.clearSession,
  })

  return {
    auth: {
      authForm: state.authForm,
      authHistory: state.authHistory,
      setAuthHistory: state.setAuthHistory,
      token: state.token,
      profile: state.profile,
      usage: state.usage,
      busy: state.busy,
      error: state.error,
      message: state.message,
      handleLogin: actions.handleLogin,
      handleLogout: actions.handleLogout,
      handleAuthFieldChange: actions.handleAuthFieldChange,
      handleSelectAuthHistory: actions.handleSelectAuthHistory,
      removeStoredProfileFromHistory: actions.removeStoredProfileFromHistory,
    },
    chrome: {
      theme: state.theme,
      historyOpen: state.historyOpen,
      setHistoryOpen: state.setHistoryOpen,
      activityOpen: state.activityOpen,
      setActivityOpen: state.setActivityOpen,
      sidebarView: state.sidebarView,
      setSidebarView: state.setSidebarView,
      statusIndicatorText: state.statusIndicatorText,
      latestUndoEntry: state.latestUndoEntry,
      toggleTheme: actions.toggleTheme,
      handleUndoAction: actions.handleUndoAction,
    },
    browser: {
      token: state.token,
      currentPath: state.currentPath,
      visibleItems: state.visibleItems,
      selectedPaths: state.selectedPaths,
      selectedItem: state.selectedItem,
      selectedItems: state.selectedItems,
      selectedFileItem: state.selectedFileItem,
      itemFilter: state.itemFilter,
      setItemFilter: state.setItemFilter,
      touchMode: state.touchMode,
      selectionMode: state.selectionMode,
      viewMode: state.viewMode,
      setViewMode: state.setViewMode,
      gridDensity: state.gridDensity,
      setGridDensity: state.setGridDensity,
      availableGridDensityOptions: state.availableGridDensityOptions,
      loading: state.loading,
      busy: state.busy,
      canRenameSelected: state.canRenameSelected,
      canDownloadSelected: state.canDownloadSelected,
      canViewDetailSelected: state.canViewDetailSelected,
      canCopyLinkSelected: state.canCopyLinkSelected,
      canOpenInBrowserSelected: state.canOpenInBrowserSelected,
      canDeleteSelected: state.canDeleteSelected,
      activeGridDensity: state.activeGridDensity,
      gridGapClass: state.gridGapClass,
      gridColumnsStyle: state.gridColumnsStyle,
      dragSourcePath: state.dragSourcePath,
      dropTargetPath: state.dropTargetPath,
      fileInputRef: state.fileInputRef,
      folderInputRef: state.folderInputRef,
      setCreateOpen: state.setCreateOpen,
      loadDirectory: actions.loadDirectory,
      toggleSelectionMode: actions.toggleSelectionMode,
      toggleSelection: actions.toggleSelection,
      handleSort: actions.handleSort,
      selectOnly: actions.selectOnly,
      openDetail: actions.openDetail,
      prepareRename: actions.prepareRename,
      prepareMove: actions.prepareMove,
      prepareMoveSelected: actions.prepareMoveSelected,
      handleCopyLink: actions.handleCopyLink,
      handleOpenInBrowser: actions.handleOpenInBrowser,
      handleDelete: actions.handleDelete,
      handleUpload: actions.handleUpload,
      handleDownload: actions.handleDownload,
      handleRowOpen: actions.handleRowOpen,
      handleItemClick: actions.handleItemClick,
      handleItemKeyDown: actions.handleItemKeyDown,
      handleDragStart: actions.handleDragStart,
      handleDragEnd: actions.handleDragEnd,
      handleDragOver: actions.handleDragOver,
      handleDragLeave: actions.handleDragLeave,
      handleDrop: actions.handleDrop,
    },
    tasks: {
      operationHistory: state.operationHistory,
      uploadTasks: state.uploadTasks,
      downloadTasks: state.downloadTasks,
      clearCompletedTasks: actions.clearCompletedTasks,
      deleteTask: actions.deleteTask,
    },
    settings: {
      profileSettings: state.profileSettings,
      settingsOpen: state.settingsOpen,
      activePublicBaseUrl: state.activePublicBaseUrl,
      domainValue: state.domainValue,
      domainError: state.domainError,
      setDomainValue: state.setDomainValue,
      setDomainError: state.setDomainError,
      saveProfileSettings: actions.saveProfileSettings,
      handleSettingsOpenChange: actions.handleSettingsOpenChange,
      handleSaveDomain: actions.handleSaveDomain,
    },
    dialogs: {
      detailOpen: state.detailOpen,
      detailItem: state.detailItem,
      detailLoading: state.detailLoading,
      detailError: state.detailError,
      detailFolderSummary: state.detailFolderSummary,
      detailPublicUrl: state.detailPublicUrl,
      detailHeadersOpen: state.detailHeadersOpen,
      setDetailHeadersOpen: state.setDetailHeadersOpen,
      detailHeaderEntries: state.detailHeaderEntries,
      moveOpen: state.moveOpen,
      moveTargets: state.moveTargets,
      moveBrowsePath: state.moveBrowsePath,
      setMoveBrowsePath: state.setMoveBrowsePath,
      moveFolders: state.moveFolders,
      moveLoading: state.moveLoading,
      canConfirmMove: state.canConfirmMove,
      createOpen: state.createOpen,
      newFolderName: state.newFolderName,
      setNewFolderName: state.setNewFolderName,
      renameOpen: state.renameOpen,
      setRenameOpen: state.setRenameOpen,
      renameValue: state.renameValue,
      setRenameValue: state.setRenameValue,
      handleDetailOpenChange: actions.handleDetailOpenChange,
      handleOpenDetailFolder: actions.handleOpenDetailFolder,
      handleMoveDialogChange: actions.handleMoveDialogChange,
      handleMoveFromDialog: actions.handleMoveFromDialog,
      handleCreateFolder: actions.handleCreateFolder,
      handleRename: actions.handleRename,
    },
  }
}
